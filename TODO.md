# Possible Future Stuff to Add

- Make a nice prompt
- Install JetBrains Mono Nerd font (`brew install font-jetbrains-mono-nerd-font`)
- GUI app installation and settings
  - Terminal app - settings (font)
  - VS Code
- Add `zmv` to autoload handlers
- Add global aliases for:
  - NE = `2>/dev/null`
  - NUL = `>/dev/null`
  - L = `| less`
